{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn wifi_billing_system.wsgi:application --bind 0.0.0.0:$PORT --workers 4"
  }
}